# e2e（尚未开始）

Playwright 端到端测试，覆盖仓库 README 9.3 中模块 A、模块 B 的主流程。

**当前状态：占位目录，没有任何实现。** 前端尚未开发，端到端测试无法编写。

## 计划覆盖的用例

| 编号 | 场景 |
| --- | --- |
| A | 创建简历 → 录入或导入 → 校正 → 保存版本 → 导出 DOCX/PDF |
| B | 选择版本 → 输入 JD → 分析 → 接受建议并排序 → 保存定向版本 → 导出 |
| I-01 | 禁用分析能力后，模块 A 仍可独立完成闭环 |

## 初始化方式（前端可运行后执行）

```powershell
cd e2e
npm init playwright@latest
```

前置条件：后端已启动（默认 `http://127.0.0.1:8000`）、前端已启动。
浏览器目标为 Edge（Chromium 内核），与 README 第 2 节的桌面端范围一致。
