# ResumeMatch backend runtime + test dependencies (Python 3.10+)
# NOTE: keep this file ASCII-only. pip on Windows decodes requirements using the
# system code page (GBK), so non-ASCII comments make the install fail.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
sqlalchemy>=2.0.36,<3.0
alembic>=1.14,<2.0
pymupdf>=1.24,<2.0
python-docx>=1.1,<2.0
python-multipart>=0.0.12,<1.0

# tests
pytest>=8.3,<9.0
httpx>=0.27,<1.0
